{
  "name": "sweet-science-systems",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend-react",
    "build:frontend": "npm run build --prefix frontend-react",
    "build": "npm run install:all && npm run build:frontend",
    "start": "npm start --prefix backend"
  }
}
